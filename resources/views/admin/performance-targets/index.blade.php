@extends('layouts.admin')
@section('content')
<h1>Performance Targets</h1><div class="card"><h2>Set Target</h2><form method="POST" action="/admin/performance-targets">@csrf<select name="employee_id"><option value="">Employee (Optional)</option>@foreach($employees as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select><select name="role_id"><option value="">Role (Optional)</option>@foreach($roles as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select><select name="department_id"><option value="">Department (Optional)</option>@foreach($departments as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select><select name="target_type">@foreach(['orders'=>'Orders','spend'=>'Spend','max_cpo'=>'Maximum CPO','approval_rate'=>'Minimum Approval Rate'] as $v=>$l)<option value="{{ $v }}">{{ $l }}</option>@endforeach</select><input type="number" step="0.01" name="target_value" placeholder="Target Value" required><select name="period_type"><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select><input type="date" name="start_date" value="{{ today()->toDateString() }}" required><input type="date" name="end_date"><input type="hidden" name="status" value="active"><button class="btn">Save Target</button></form></div>
<div class="card"><table><thead><tr><th>Scope</th><th>Target</th><th>Period</th><th>Dates</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($targets as $target)<tr><td>{{ $target->employee?->name ?: $target->role?->name ?: $target->department?->name ?: 'General' }}</td><td>{{ ucwords(str_replace('_',' ',$target->target_type)) }}: {{ number_format($target->target_value,2) }}</td><td>{{ ucfirst($target->period_type) }}</td><td>{{ $target->start_date?->toDateString() }} - {{ $target->end_date?->toDateString() ?: 'Open' }}</td><td>{{ ucfirst($target->status) }}</td><td><form method="POST" action="/admin/performance-targets/{{ $target->id }}">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</tbody></table></div>
@endsection
