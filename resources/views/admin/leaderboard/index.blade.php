@extends('layouts.admin')
@section('content')
<h1>Employee Leaderboard</h1><form class="card" method="GET"><select name="type">@foreach(['orders'=>'Top Moderators by Orders','approval'=>'Top by Approval Rate','spend'=>'Top Ad Managers by Spend','cpo'=>'Lowest CPO','consistency'=>'Top by Consistency'] as $value=>$label)<option value="{{ $value }}" {{ $type===$value?'selected':'' }}>{{ $label }}</option>@endforeach</select><input type="date" name="date_from" value="{{ $from->toDateString() }}"><input type="date" name="date_to" value="{{ $to->toDateString() }}"><button class="btn">Apply</button><a class="btn" href="/admin/leaderboard/export?type={{ $type }}&date_from={{ $from->toDateString() }}&date_to={{ $to->toDateString() }}">Export CSV</a></form>
<div class="card"><table><thead><tr><th>Rank</th><th>Employee</th><th>Role</th><th>Department</th><th>Metric</th><th>Trend</th></tr></thead><tbody>@foreach($rows as $row)<tr><td>#{{ $loop->iteration }}</td><td>{{ $row['employee']->name }}</td><td>{{ $row['employee']->roleName() }}</td><td>{{ $row['employee']->departmentName() }}</td><td>{{ number_format($row['metric_value'],2) }}</td><td>{{ $row['consistency'] >= 80 ? 'Stable' : 'Needs Attention' }}</td></tr>@endforeach</tbody></table></div>
@endsection
